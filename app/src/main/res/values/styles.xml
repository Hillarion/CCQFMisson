<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- you should set typeface which you want to override with TypefaceUtil -->
        <item name="android:typeface">monospace</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.ActionBarLogo" parent="Theme.AppCompat.Light">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <item name="android:background">#ffffffff</item>
        <item name="logo">@mipmap/ccqf_logo</item>
        <item name="android:actionBarSize">35dp</item>
        <item name="actionBarSize">75dp</item>
        <!--item name="android:alignmentMode">alignBounds</item-->
        <item name="displayOptions">useLogo</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"
        />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--<style name="MyCustomTheme" parent="android:Holo.SegmentedButton">
        <item name="android:actionOverflowButtonStyle">@style/Widget.AppCompat.Button</item>
    </style>

    <style name="MyCustomTheme.OverFlow">
        <item name="android:src">@drawable/my_overflow_image</item>
    </style>-->

</resources>
